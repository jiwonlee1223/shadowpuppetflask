# 영구 활성화 기능 가이드 ⭐

## 개요

**영구 활성화(Permanent Activation)** 기능은 토끼 형태를 3초 이상 탐지하면, 이후에 형태가 사라져도 비디오가 계속 재생되도록 하는 기능입니다.

## 작동 방식

### 1단계: 초기 탐지
- 웹캠에 토끼 형태를 보여줍니다
- 형태가 인식되면 **"잠금"** 상태가 됩니다 (녹색 배지)

### 2단계: 영구 활성화 (3초 후)
- 잠금 상태가 **3초 이상** 유지되면
- 자동으로 **"영구 활성"** 모드로 전환됩니다 (노란색 배지 ⭐)

### 3단계: 지속 재생
- 영구 활성 모드가 되면:
  - ✅ 토끼 형태를 치워도 비디오가 계속 재생됩니다
  - ✅ 마지막으로 탐지된 위치에 비디오가 고정됩니다
  - ✅ 토끼 형태를 다시 보여주면 위치가 업데이트됩니다

### 4단계: 리셋
- **"리셋" 버튼**을 클릭하면 영구 활성화가 해제됩니다
- 처음부터 다시 시작할 수 있습니다

## 상태 표시

| 상태 | 배지 색상 | 의미 |
|------|-----------|------|
| **잠금 해제** | 회색 🔓 | 형태 탐지 대기 중 |
| **잠금** | 녹색 🔒 | 형태 탐지됨 (0-3초) |
| **영구 활성** | 노란색 ⭐ | 3초 이상 탐지 완료, 계속 재생 |

## 활용 예시

### 시나리오 1: 프레젠테이션
```
1. 토끼 종이를 3초간 보여줌
2. 영구 활성화 ⭐
3. 토끼 종이를 치우고 프레젠테이션 진행
4. 비디오는 계속 재생됨!
```

### 시나리오 2: 퍼포먼스
```
1. 토끼 모양 손동작을 3초간 유지
2. 영구 활성화 ⭐
3. 자유롭게 움직여도 비디오는 계속 재생
4. 필요 시 다시 토끼 모양으로 위치 조정
```

### 시나리오 3: 전시
```
1. 관람객이 토끼 모양 전시물을 3초간 보여줌
2. 영구 활성화 ⭐
3. 전시물을 치워도 AR 효과 지속
4. 다음 관람객이 올 때까지 유지
```

## 기술적 세부사항

### 타이머 설정

`shape_detector.py`에서 영구 활성화 시간을 조정할 수 있습니다:

```python
# 초기화 코드
self.permanent_activation_time = 3.0  # 3초 (조정 가능)
```

**권장 설정:**
- **빠른 활성화:** 2.0초
- **기본 (권장):** 3.0초 ⭐
- **신중한 활성화:** 5.0초

### 알고리즘

```python
# 1. 잠금 시작 시간 기록
if self.good_frames >= self.lock_count_enter:
    self.is_locked = True
    self.locked_start_time = time.time()  # 현재 시간 저장

# 2. 경과 시간 확인
elapsed = time.time() - self.locked_start_time
if elapsed >= self.permanent_activation_time:
    self.is_permanently_active = True  # 영구 활성화!

# 3. 마지막 유효 결과 저장
if self.is_permanently_active:
    if 탐지_성공:
        self.last_valid_result = 현재_결과  # 업데이트
    else:
        return self.last_valid_result  # 마지막 결과 반환
```

### 데이터 흐름

```
[형태 탐지] → [잠금] → [3초 경과] → [영구 활성화]
                ↓                         ↓
          [시간 측정 시작]      [마지막 결과 저장]
                                          ↓
                                  [탐지 실패해도 계속 반환]
```

## 상태 전환 다이어그램

```
        시작
          ↓
    [잠금 해제] ←──── (리셋 버튼)
          ↓
    (형태 탐지 12프레임)
          ↓
      [잠금] ←──────┐
          ↓         │
    (3초 경과)      │ (형태 재탐지)
          ↓         │
   [영구 활성] ─────┘
          ↓
   (형태 없어도 계속 재생)
```

## UI 변화

### HTML 배지 표시

```html
<!-- 잠금 해제 -->
<span class="badge bg-secondary">
    <i class="fas fa-unlock"></i> 잠금 해제
</span>

<!-- 잠금 -->
<span class="badge bg-success">
    <i class="fas fa-lock"></i> 잠금
</span>

<!-- 영구 활성 -->
<span class="badge bg-warning text-dark">
    <i class="fas fa-star"></i> 영구 활성
</span>
```

### JavaScript 처리

```javascript
if (detection.is_permanently_active) {
    lockStatus.innerHTML = '<i class="fas fa-star me-1"></i>영구 활성';
    lockStatus.className = 'badge bg-warning text-dark';
} else if (detection.is_locked) {
    lockStatus.innerHTML = '<i class="fas fa-lock me-1"></i>잠금';
    lockStatus.className = 'badge bg-success';
} else {
    lockStatus.innerHTML = '<i class="fas fa-unlock me-1"></i>잠금 해제';
    lockStatus.className = 'badge bg-secondary';
}
```

## 문제 해결

### 영구 활성화가 너무 빨리 됩니다
```python
# shape_detector.py
self.permanent_activation_time = 5.0  # 3.0 → 5.0으로 증가
```

### 영구 활성화가 안 됩니다
1. **3초 동안 형태를 안정적으로 유지**해야 합니다
2. 잠금 상태(녹색)가 3초간 유지되는지 확인
3. 중간에 탐지가 끊기면 타이머가 리셋됩니다

### 리셋이 안 됩니다
```javascript
// 리셋 버튼 확인
document.getElementById('btn-reset').addEventListener('click', resetDetector);

function resetDetector() {
    socketHandler.resetDetector();
}
```

### 위치가 업데이트 안 됩니다
- 영구 활성 모드에서도 토끼 형태를 다시 보여주면 위치가 업데이트됩니다
- 탐지가 잘 안 되면 레벨 조정을 사용하세요

## 성능 영향

### 메모리 사용
- 마지막 유효 결과 저장: **< 1KB**
- 거의 무시 가능한 수준

### CPU 사용
- 추가 처리 없음
- 오히려 탐지 실패 시 재처리하지 않아 **효율적**

### 네트워크
- 영구 활성 상태 플래그만 추가: **1 byte**
- 영향 없음

## 고급 기능 (향후 확장 가능)

### 1. 자동 해제 타이머
```python
# N초 후 자동 해제
self.permanent_activation_duration = 30.0  # 30초
```

### 2. 여러 형태 저장
```python
# 여러 위치에 동시에 비디오 재생
self.saved_results = []  # 리스트로 저장
```

### 3. 제스처 트리거
```python
# 특정 제스처로 활성화/해제
if gesture == "peace_sign":
    self.is_permanently_active = not self.is_permanently_active
```

## 요약

✅ **3초 탐지** → ⭐ **영구 활성화** → 🎬 **계속 재생**

이 기능으로 더욱 편리하고 자연스러운 AR 경험을 즐기세요!

---

**궁금한 점이 있으면 README.md를 참고하세요.**

